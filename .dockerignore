# Dependencies
node_modules
**/node_modules

# Build artifacts
**/dist
**/build
**/.next
**/out
**/.turbo

# TypeScript incremental cache (confunde buildkit cache mounts)
**/*.tsbuildinfo

# Env
# Recursivo: `veloz deploy` sobe o diretorio local, e os padroes sem `**/`
# so casavam na raiz. `packages/web/.env.local` subia junto e sobrescrevia o
# API_URL_INTERNAL de producao pela URL publica -- todo fetch server-side
# passava a sair pela internet.
**/.env
**/.env.local
**/.env.*.local

# Logs & editor
**/*.log
.DS_Store
.vscode
.idea

# Git
.git
.gitignore

# Tests
**/playwright-report
**/test-results
**/coverage

# Session notes
NEXT_SESSION.md
